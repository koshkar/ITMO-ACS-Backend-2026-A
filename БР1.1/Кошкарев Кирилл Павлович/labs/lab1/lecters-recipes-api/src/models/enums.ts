export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
}

export enum RecipeStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ARCHIVED = 'archived',
}

export enum RecipeDifficulty {
  EASY = 'easy',
  MEDIUM = 'medium',
  HARD = 'hard',
}

export enum MediaType {
  PHOTO = 'photo',
  VIDEO = 'video',
}
